import { NameShortcutPipe } from './name-shortcut.pipe';

describe('NameShortcutPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShortcutPipe();
    expect(pipe).toBeTruthy();
  });
});
